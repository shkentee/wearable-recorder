diff --git a/omi/firmware/devkit/src/main.c b/omi/firmware/devkit/src/main.c
index e8df45d..94a1440 100644
--- a/omi/firmware/devkit/src/main.c
+++ b/omi/firmware/devkit/src/main.c
@@ -298,19 +298,8 @@ int main(void)
 
     set_led_blue(true);
 
-    // Audio codec(opus) callback
-    set_codec_callback(codec_handler);
-    err = codec_start();
-    if (err) {
-        LOG_ERR("Failed to start codec: %d", err);
-        // Blink blue LED to indicate error
-        for (int i = 0; i < 5; i++) {
-            set_led_blue(!gpio_pin_get_dt(&led_blue));
-            k_msleep(200);
-        }
-        set_led_blue(false);
-        return err;
-    }
+    // v22: skip codec/mic init for BLE establishment test
+    LOG_INF("v22: codec_start() skipped (establishment test)");
 
 #ifdef CONFIG_OMI_ENABLE_HAPTIC
     play_haptic_milli(500);
@@ -324,20 +313,7 @@ int main(void)
     set_led_red(true);
     set_led_green(true);
 
-    set_mic_callback(mic_handler);
-    err = mic_start();
-    if (err) {
-        LOG_ERR("Failed to start microphone: %d", err);
-        // Blink red and green LEDs to indicate error
-        for (int i = 0; i < 5; i++) {
-            set_led_red(!gpio_pin_get_dt(&led_red));
-            set_led_green(!gpio_pin_get_dt(&led_green));
-            k_msleep(200);
-        }
-        set_led_red(false);
-        set_led_green(false);
-        return err;
-    }
+    LOG_INF("v22: mic_start() skipped (establishment test)");
 
     set_led_red(false);
     set_led_green(false);
